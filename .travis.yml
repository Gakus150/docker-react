language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t Gakus150/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true Gakus150/docker-react npm run test